apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPartialPolicy
metadata:
  name: phenv-01hq97r5kca95vdv909vj07vfn-iampolicy # kpt-set: ${project-dept}${project-env}-${project-unique-id}-iampolicy
  namespace: "config-control"
  annotations:
    cnrm.cloud.google.com/blueprint: 'kpt-fn'
  labels:
    vanity-name: "phenv-testProject" # kpt-set: ${project-dept}${project-env}-${project-vanity-name}
    classification: exp # kpt-set: ${project-classification}
spec:
  bindings:
    - role: roles/owner
      members: # kpt-set: ${iam-owners}
        - member: "user:fname.lname@gcp.hc-sc.gc.ca"
        - member: "user:momo"
        - member: "user:rick"
        - member: "user:morty"
  resourceRef:
    apiVersion: resourcemanager.cnrm.cloud.google.com/v1beta1
    external: projects/phenv-01hq97r5kca95vdv909vj07vfn # kpt-set: projects/${project-dept}${project-env}-${project-unique-id}
    kind: Project
