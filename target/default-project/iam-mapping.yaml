apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPartialPolicy
metadata:
  name: deptenv-01hq9jpysjmq8pt5g636vyxms3-iampolicy # kpt-set: ${project-dept}${project-env}-${project-unique-id}-iampolicy
  namespace: "config-control"
  annotations:
    cnrm.cloud.google.com/blueprint: 'kpt-fn'
  labels:
    vanity-name: "deptenv-test-project" # kpt-set: ${project-dept}${project-env}-${project-vanity-name}
    classification: classification # kpt-set: ${project-classification}
spec:
  bindings:
    - role: roles/owner
      members: # kpt-set: ${iam-owners}
        - member: "user:fname.lname@gcp.hc-sc.gc.ca"
        - member: "user:momo"
        - member: "user:rick"
        - member: "user:morty"
  resourceRef:
    apiVersion: resourcemanager.cnrm.cloud.google.com/v1beta1
    external: projects/deptenv-01hq9jpysjmq8pt5g636vyxms3 # kpt-set: projects/${project-dept}${project-env}-${project-unique-id}
    kind: Project
