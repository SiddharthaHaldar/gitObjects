apiVersion: resourcemanager.cnrm.cloud.google.com/v1beta1
kind: Project
metadata:
  # This should be the same as project ID
  name: "phenv-1234567n" # kpt-set: ${project-dept}${project-env}-${project-unique-id}
  namespace: "config-control"
  labels:
    vanity-name: "phenv-idc" # kpt-set: ${project-dept}${project-env}-${project-vanity-name}
    classification: idfc # kpt-set: ${project-classification}
  annotations:
    cnrm.cloud.google.com/blueprint: 'kpt-fn'
spec:
  name: phenv-idc # kpt-set: ${project-dept}${project-env}-${project-vanity-name}
  billingAccountRef:
    external: "016B6D-6AB281-505940" # kpt-set: ${billing-id}
  folderRef:
    name: ph-1234567n-folder # kpt-set: ${project-dept}-${project-unique-id}-folder
